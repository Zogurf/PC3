/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package POO;

/**
 *
 * @author DELL
 */
public class teoriaXD {
    
    // 
    // modelo: clases base, metodos; solo aca se cambia el codigo
    // controlador: nexo entre modelo y vista, ademas de establecer la intervecion de varios modelos a la vez
    // vista: interfaz del usuario, aca va el metodo main   
    
    // metodos void no devuelven valor, otros por eje: public int contar() {return 0;} devuelve por el tipo de dato (debe llevar retorno)
    // se debe importar el paquete y clase
    
    // tipos de dato inyectivos int, bootlean, etc.
    // tipo de dato referenciado String, scanner, etc.
    
    // Es el plano del objeto, tiene atributos (estado) y comportamientos (metodos)
    
    // public desde el mismo paquete y en otro paquete, no marca problema
    // private marca problema en el mismo paquete y en otro, solo se puede usar dentro de su package
    // protected marca problema en otro paquete, pero si en el mismo paquete
    
    // en UML + es publico, - privado y # privado
    // los public no es necesario hacer set
    
    // @override para metodo toString para sobrescribir una variable a texto, convertir cualquier variable a texto, toString es funcion (return)
    // public String toString() {
    //  return nombre + " - " + eda;
    // se puede usar toString() equals(), hashcode()
    
    // metodo de instancia, se crea el objeto a partir de la definicion de la clase
    // cuando es array no se pued usar el =
    
    // Metodos -> Procedimiento:  no devuelve valor (void, no necesita retorno pero si se puede usar, si se usa se sale del metodo)
    // Metodos -> Funcion:  retorna valor (return)
    
    // En sobrecarga de constructores permite definir nuevas versiones de un mismo metodo, se pueden usar varios metodos con el mismo nombre, deben cambiar los parametros
}
